using static debugging.message;

namespace debugging
{
    class program
    {
         static void Main(string[] args)
         {
            string message = Message.GetMessage("Yb");
                
              Console.WriteLine(message);
                    
                
         }
    }
}